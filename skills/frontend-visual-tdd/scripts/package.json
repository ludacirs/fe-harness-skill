{
  "name": "frontend-visual-tdd",
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone visual TDD scripts — Playwright screenshot capture + pixelmatch diff",
  "scripts": {
    "setup": "npm install && npx playwright install chromium --with-deps"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "^1.40.0",
    "pngjs": "^7.0.0"
  }
}
