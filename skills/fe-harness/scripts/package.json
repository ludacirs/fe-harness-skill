{
  "name": "fe-harness-scripts",
  "version": "1.0.0",
  "type": "module",
  "description": "Frontend harness scripts — Playwright capture + pixelmatch diff + Figma export",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "setup": "npm install && npx playwright install chromium --with-deps",
    "capture": "tsx capture.ts",
    "diff": "tsx diff.ts",
    "figma-export": "tsx figma-export.ts"
  },
  "dependencies": {
    "pixelmatch": "^6.0.0",
    "playwright": "^1.40.0",
    "pngjs": "^7.0.0",
    "tsx": "^4.0.0"
  }
}
