[
  {
    "_comment": "step 1: initial page load state — no action needed",
    "waitForState": "networkidle"
  },
  {
    "_comment": "step 2: fill email field",
    "fill": { "selector": "input[name='email']", "value": "test@example.com" }
  },
  {
    "_comment": "step 3: fill password field",
    "fill": { "selector": "input[name='password']", "value": "password123" }
  },
  {
    "_comment": "step 4: submit and wait for result",
    "click":   "button[type='submit']",
    "waitFor": ".dashboard-header"
  },
  {
    "_comment": "step 5: dashboard loaded state",
    "waitForState": "networkidle"
  }
]
